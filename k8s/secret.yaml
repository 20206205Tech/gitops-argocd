apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: doppler-secret
  namespace: default
spec:
  tokenSecret:
    name: doppler-token-secret
  managedSecret:
    name: secrets
    namespace: default
    type: Opaque